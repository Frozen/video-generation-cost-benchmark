# Preserve the exact upstream prompt README, including its original whitespace.
source/PROMPTS_README.md -whitespace

# Reproducibility evidence uses byte-level hashes; preserve original line endings.
benchmarks/ltx-rtx-2026-09-20/** -text

# Keep exported Vast evidence byte-identical, including the CSV writer's CRLF.
results/*VAST* -text
results/*VAST*.csv whitespace=cr-at-eol
benchmarks/ltx-sustained-2026-09-21/** -text

# Preserve intentional trailing spaces inside verbatim multiline benchmark prompts.
benchmarks/ltx-sustained-2026-09-21/results/VIDEO_CATALOG.csv whitespace=-blank-at-eol
